syntax = "proto3";
option go_package = "github.com/twirapp/twir/libs/grpc/streamstatus";
package streamstatus;

service Eval {
  rpc Process(Evaluate) returns (EvaluateResult) {}
}

message Evaluate {
  string script = 1;
}

message EvaluateResult {
  string result = 1;
}
