{
  "name": "@tsuwari/prisma",
  "description": "",
  "type": "module",
  "module": "./dist/index.js",
  "main": "./dist/index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b tsconfig.json",
    "symlink": "ln -s ../../apps/backend/.env .env",
    "generate": "prisma generate",
    "deploy": "prisma migrate deploy",
    "prisma": "doppler run -- prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^8.4.5",
    "@prisma/client": "^4.0.0",
    "prisma": "^4.0.0"
  }
}