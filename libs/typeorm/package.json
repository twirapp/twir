{
  "name": "@tsuwari/typeorm",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./entities/*": "./dist/entities/*.js"
  },
  "typesVersions": {
    "*": {
      "*": [
        "./dist/*.d.ts"
      ]
    }
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "cli": "doppler run -- typeorm-ts-node-esm -d ./src/index.ts",
    "deploy": "pnpm typeorm-ts-node-esm -d ./src/index.ts migration:run",
    "migration:generate": "doppler run -- pnpm ts-node --esm --transpileOnly ./generateMigration.ts",
    "setup": "pnpm run deploy && pnpm run seed",
    "seed": "pnpm ts-node --esm --transpileOnly ./seed.ts"
  },
  "dependencies": {
    "@tsuwari/config": "workspace:*",
    "pg": "8.8.0",
    "reflect-metadata": "^0.1.13",
    "ts-node": "10.9.1",
    "typeorm": "^0.3.10",
    "typescript": "4.8.4"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
}