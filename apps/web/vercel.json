{
  "github": {
    "silent": true
  },
  "buildCommand": "npx turbo run build --filter=web --cwd=../..",
  "builds": [
    {
      "src": "./dist/server/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
