{
  "name": "@twir/executron",
  "private": true,
  "scripts": {
    "dev": "bun run --env-file=../../.env --watch src/index.ts",
    "start": "bun run --env-file=../../.env src/index.ts",
    "build": "tsc --noEmit && bun run build.ts"
  },
  "dependencies": {
    "@isolated-vm/experimental": "0.0.9",
    "@twir/bus-core": "workspace:*",
    "@twir/config": "workspace:*",
    "lodash": "4.17.21",
    "nats": "2.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
