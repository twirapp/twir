{
  "name": "@twir/executron",
  "private": true,
  "scripts": {
    "dev": "bun run --env-file=../../.env --watch src/index.ts",
    "start": "bun run --env-file=../../.env src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile=.out/twir-executron"
  },
  "dependencies": {
    "@twir/config": "workspace:*",
    "@isolated-vm/experimental": "0.0.8",
    "@twir/bus-core": "workspace:*",
    "nats": "2.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
