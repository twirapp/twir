# This template is intentionally not referenced by kustomization.yaml.
# Do not commit real values here. Prefer creating the runtime Secret from secrets.example.env:
# kubectl -n twir create secret generic twir-secrets --from-env-file=/tmp/twir-secrets.env
apiVersion: v1
kind: Secret
metadata:
  name: twir-secrets
  namespace: twir
type: Opaque
stringData: {}
