# Intent Diff: Multiplatform Robustness

| intent_id | Expected truth | Observed reality | Diff | Violated invariant | Intent source | Supporting observations | Status | Claims |
|---|---|---|---|---|---|---|---|---|
| I1 | Every platform uses the same internal channel/user/message identity contracts while retaining provider IDs separately. | Runtime already exposed a numeric VK message ID written into an internal UUID field. | Open | Internal IDs must be provider-agnostic UUIDs. | User request + runtime logs | O1 | violated | C1 |
| I2 | Platform capabilities accurately describe implemented ingress/outbound features. | VK ingestion is implemented while `PlatformVKVideoLive` advertises only `chat.write`; Kick lacks `chat.read` despite ingestion code. | Open | Capability registry must match runtime support. | Platform abstraction design | O2 | violated | C2 |
| I3 | Parser, variables, commands, events, timers, overlays, and moderation route by platform/binding rather than Twitch aliases. | Parser DB-channel lookup, greetings, event ban gate, profile routing, and selector surfaces retain Twitch-hardcoded behavior; VK timer exclusion is intentional. | Open | No Twitch-only identifier may be used as a universal channel/user key. | User request | O6 | violated | C6,C10,C12,C17 |
| I4 | Outbound adapters use documented provider APIs and fail with typed, actionable errors. | VK outbound uses undocumented `/v1/channels/active` and runtime returned 404. | Open | Provider calls must follow first-party contract. | Runtime logs + official docs | O3 | violated | C3 |
| I5 | Persistence schemas accept all provider-native identifier formats without corrupting cross-platform semantics. | Chat ingress normalizes internal IDs correctly, but VK provider message IDs still reach UUID persistence and event `ChannelPlatformID` has split semantics. | Open | External IDs and internal IDs remain distinct at every persistence boundary. | User request | O1,O4 | violated | C1,C2 |
| I6 | Tests exercise each supported platform through ingress, parser, outbound, persistence, and UI capability exposure. | VK timer/no-bot behavior is explicitly tested, while parser response races, unban workflow dispatch, greetings, rate-limit semantics, and VK profile routing lack focused coverage; committed Kick split tests fail. | Open | Each platform-feature cell has a regression test or explicit unsupported declaration. | User request | O5,O6,O8 | violated | C3,C7,C9,C12,C13,C17,C19 |
