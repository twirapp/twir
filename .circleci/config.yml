version: 2.1

jobs:
  build-base:
    docker:
      - image: cimg/base:stable
    parameters:
      target:
        type: string
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build application Docker image
          command: |
            docker build -f base.Dockerfile --target << parameters.target >> --tag twir-base:<< parameters.target >> .
  build-backend:
    docker:
      - image: cimg/base:stable
    parameters:
      target:
        type: string
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build application Docker image
          command: |
            docker build -f ./apps/<< parameters.target >> --tag twir:<< parameters.target >> .


workflows:
  build-docker-images:
    jobs:
      - build-base:
          name: build-base-images
          matrix:
            parameters:
              target:
                - builder
                - node_prod_base
                - go_prod_base
      - build-backend:
          name: build-backend
          requires:
            - build-base-images-<< parameters.base >>
          matrix:
            parameters:
              base:
                - builder
                - node_prod_base
                - go_prod_base
              target:
                - api
                - bots
                - discord
                - emotes-cacher
                - eval
                - events
                - eventsub
                - integrations
                - language-detector
                - parser
                - scheduler
                - timers
                - tokens
                - websockets
                - ytsr

