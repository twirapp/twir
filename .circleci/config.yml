version: 2.1

jobs:
  build-base:
    name: build-base-images << parameters.target >>
    docker:
      - image: cimg/base:stable
    parameters:
      target:
        type: string
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build application Docker image
          command: |
            docker build -f base.Dockerfile --target << parameters.target >> --tag twir-base:<< parameters.target >> .
  build-backend:
    docker:
      - image: cimg/base:stable
    parameters:
      target:
        type: string
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build application Docker image
          command: |
            docker build -f ./apps/<< parameters.target >> --tag twir:<< parameters.target >> .


workflows:
  build-base-images:
    jobs:
      - build-base:
          name: build-base-images
          matrix:
            parameters:
              target:
                - builder
                - node_prod_base
                - go_prod_base
      - build-backend:
          requires:
            - build-base-images
          name: build-backend
          matrix:
            parameters:
              target:
                - api
                - bots
                - discord
                - emotes-cacher
                - eval
                - events
                - eventsub
                - integrations
                - language-detector
                - parser
                - scheduler
                - timers
                - tokens
                - websockets
                - ytsr
